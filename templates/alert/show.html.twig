{% extends 'base.html.twig' %}

{% block title %}{{ alert.titre }} - Nation Sounds{% endblock %}

{% block body %}

    <div id="background-slider" style="background-image: url({{ alert.photo }});">

                <h1>{{ alert.titre }}</h1>
</div>
    <div id="background-alert">
    <div class="background-alert-text">
                <p>{{ alert.text }}</p>


                <img src="{{ alert.photo2 }}"/>
    </div>
    </div>
{% endblock %}
